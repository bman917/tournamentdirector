<%= form_for(@game_type) do |f| %>
  <% if @game_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game_type.errors.count, "error") %> prohibited this game_type from being saved:</h2>

      <ul>
      <% @game_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :number_of_players %><br />
    <%= f.number_field :number_of_players %>
  </div>
  <div class="field">
    <%= f.label :number_of_games %><br />
    <%= f.number_field :number_of_games %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

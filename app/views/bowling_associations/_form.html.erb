<%= form_for(@bowling_association) do |f| %>
  <% if @bowling_association.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bowling_association.errors.count, "error") %> prohibited this bowling_association from being saved:</h2>

      <ul>
      <% @bowling_association.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

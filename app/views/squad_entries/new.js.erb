squadsJS_removeNewRow(); // on squds.js
$('#header_row').after("<%=j render 'form_remote'%>");
$('.ui-autocomplete').remove();
$('.bowler_name').autocomplete({
        source: $('.bowler_name').data('autocomplete-source'),
        minLength: 3
      }
    );
$('.bowler_name:first').val("").focus();

$(document).on('submit', '#new_squad_entry', showAddingRow);

$(document).on('click', '#hide_button', function(event) {
	event.preventDefault();
	squadsJS_removeNewRow(); //on squads.js
});

$('.entries_actions').show();
$('.entries_actions_disabled').hide();
$('#loading_form').hide();